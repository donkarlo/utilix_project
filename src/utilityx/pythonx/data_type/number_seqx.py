from typing import Sequence
from numbers import Number

class NumberSeqx(Seqx):
    def __init__(self, seq:Sequence[Numer,...]):
        '''
        Represents number sequences as the main
        Args:
            seq:
        '''
        # TODO: check if all are numbers
        super().__init__(seq)